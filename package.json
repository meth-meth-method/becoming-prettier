{
  "name": "becoming-prettier",
  "scripts": {
    "prettier": "prettier '*/**/*.{js,jsx}'",
    "format:auto": "npm run prettier -- --write",
    "format:check": "npm run prettier -- --list-different"
  },
  "dependencies": {
    "prettier": "^1.7.4"
  }
}
